package com.redhawk.wallet.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand ──────────────────────────────────────────────────────────────────────
val RedHawkRed       = Color(0xFFBE1E2D)   // deeper, richer crimson
val RedHawkRedDark   = Color(0xFF8C0E1A)   // pressed / gradient end
val RedHawkRedLight  = Color(0xFFD94455)   // hover / gradient start
val RedHawkGold      = Color(0xFFCFAD60)   // warm gold accent
val RedHawkGoldLight = Color(0xFFE8CC8A)   // gold highlight

// ── Light scheme ──────────────────────────────────────────────────────────────
val BackgroundLight  = Color(0xFFF5F4F2)   // warm off-white, not clinical
val SurfaceLight     = Color(0xFFFFFFFF)
val SurfaceVariantL  = Color(0xFFEEECE9)
val TextPrimary      = Color(0xFF1A1612)   // warm near-black
val TextSecondary    = Color(0xFF6B6560)

// ── Dark scheme ───────────────────────────────────────────────────────────────
val BackgroundDark   = Color(0xFF100E0D)   // near-black with warmth
val SurfaceDark      = Color(0xFF1C1917)   // elevated surface
val SurfaceVariantD  = Color(0xFF2A2622)   // cards, inputs
val OnSurfaceDark    = Color(0xFFEDE8E3)   // warm white text
val OnSurfaceVarD    = Color(0xFF9E9590)   // muted text

// ── Semantic ──────────────────────────────────────────────────────────────────
val ErrorRed         = Color(0xFFCF3A3A)
val SuccessGreen     = Color(0xFF4CAF76)